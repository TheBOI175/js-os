// ─── JS OS Global Config ───
// Change app names here — they propagate everywhere automatically.
window.JSOS_CONFIG = {
    os:   'JS OS',
    chat: 'JS Chat',
    note: 'JS Note',
    tube: 'JS Tube',
    call: 'JS Call',
};
